# Configuracion centralizada de ms-vehicle (servida por Config Server)
server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_vehicle?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "12345678"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
